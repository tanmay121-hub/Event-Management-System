spring.application.name=Backend

# =====================
# DATABASE CONFIG
# =====================
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/Backend?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:sql12826894}
spring.datasource.password=${DB_PASSWORD:pass123GHQPugmnlp}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =====================
# JPA CONFIG
# =====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =====================
# FLYWAY CONFIG
# =====================
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.locations=classpath:db/migration

# =====================
# MAIL CONFIG
# =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:jaiswalamol83@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dvgitdpuwvpzwqvn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =====================
# SERVER CONFIG
# =====================
server.port=${PORT:8080}

# =====================
# JWT CONFIG
# =====================
jwt.secret=${JWT_SECRET:TvKkrirg4G0+ZsAowRJ64cunfOM2yEJM7TcHmu1HBXA=}
jwt.expiration=${JWT_EXPIRATION:3600000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}